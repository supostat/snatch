// Commands will be added as services are implemented:
// pub mod download;   // yt_get_info, yt_download, yt_cancel
// pub mod settings;   // settings_get_all, settings_get, settings_set
// pub mod history;    // history_get_all, history_add, history_remove, history_clear
// pub mod dialog;     // select_folder, open_path, open_url, show_in_folder
// pub mod window;     // window_minimize, window_maximize, window_close
